# 🌐 ГАЙД ПО РАЗМЕЩЕНИЮ САЙТА В ИНТЕРНЕТЕ

## Подготовка к размещению

1. Убедитесь, что все данные в `src/data.js` обновлены
2. Проверьте работу сайта локально: `npm run dev`
3. Создайте продакшен сборку: `npm run build`

После выполнения `npm run build` появится папка `dist` - это ваш готовый сайт.

---

## 🚀 Вариант 1: Netlify (РЕКОМЕНДУЕТСЯ)

### Преимущества:
✅ Бесплатный хостинг
✅ Автоматический HTTPS
✅ Быстрый CDN
✅ Простой деплой

### Шаги:

1. **Зарегистрируйтесь на Netlify**
   - Перейдите на https://netlify.com
   - Войдите через GitHub/GitLab/Email

2. **Деплой методом Drag & Drop**
   ```bash
   npm run build
   ```
   - Откройте https://app.netlify.com/drop
   - Перетащите папку `dist` в окно браузера
   - Готово! Ваш сайт онлайн за 30 секунд

3. **Или через Git (автоматический деплой)**
   - Создайте репозиторий на GitHub
   - Загрузите туда проект
   - На Netlify: "New site from Git"
   - Выберите ваш репозиторий
   - Build command: `npm run build`
   - Publish directory: `dist`

---

## 🔷 Вариант 2: Vercel

### Преимущества:
✅ Бесплатный хостинг
✅ Очень быстрый
✅ Простая настройка

### Шаги:

1. Зарегистрируйтесь на https://vercel.com
2. Выполните:
   ```bash
   npm install -g vercel
   npm run build
   vercel --prod
   ```
3. Следуйте инструкциям в терминале

---

## 📦 Вариант 3: GitHub Pages (Бесплатно)

### Шаги:

1. **Установите gh-pages**
   ```bash
   npm install --save-dev gh-pages
   ```

2. **Обновите package.json**
   
   Добавьте в начало файла:
   ```json
   "homepage": "https://ваш-username.github.io/название-репозитория",
   ```
   
   Добавьте в "scripts":
   ```json
   "predeploy": "npm run build",
   "deploy": "gh-pages -d dist"
   ```

3. **Деплой**
   ```bash
   npm run deploy
   ```

4. **Настройте GitHub**
   - Репозиторий → Settings → Pages
   - Source: gh-pages branch
   - Сохранить

---

## 🌍 Вариант 4: Hostinger / Beget / Другой хостинг

### Для обычного хостинга:

1. **Создайте продакшен сборку**
   ```bash
   npm run build
   ```

2. **Загрузите файлы**
   - Подключитесь по FTP
   - Загрузите содержимое папки `dist` в корень сайта
   - Обычно это папка `public_html` или `www`

3. **Настройте .htaccess** (для Apache)
   
   Создайте файл `.htaccess` в корне:
   ```apache
   <IfModule mod_rewrite.c>
     RewriteEngine On
     RewriteBase /
     RewriteRule ^index\.html$ - [L]
     RewriteCond %{REQUEST_FILENAME} !-f
     RewriteCond %{REQUEST_FILENAME} !-d
     RewriteRule . /index.html [L]
   </IfModule>
   ```

---

## 📱 Вариант 5: Firebase Hosting

### Шаги:

1. **Установите Firebase CLI**
   ```bash
   npm install -g firebase-tools
   ```

2. **Инициализация**
   ```bash
   firebase login
   firebase init hosting
   ```
   
   Выберите:
   - Public directory: `dist`
   - Single-page app: `Yes`
   - Automatic builds: `No`

3. **Деплой**
   ```bash
   npm run build
   firebase deploy
   ```

---

## 🎯 ЧЕКЛИСТ ПЕРЕД ПУБЛИКАЦИЕЙ

- [ ] Обновлены все данные в `data.js`
- [ ] Заменены все фотографии
- [ ] Проверены ссылки на соцсети
- [ ] Обновлены контакты
- [ ] Проверена работа на мобильных
- [ ] Изменен заголовок в `index.html`
- [ ] Добавлено описание сайта (meta description)
- [ ] Все ссылки на фото работают
- [ ] Форма контактов настроена

---

## 🔧 НАСТРОЙКА ФОРМЫ ОБРАТНОЙ СВЯЗИ

### Вариант 1: EmailJS (Бесплатно, 200 писем/месяц)

1. Зарегистрируйтесь на https://www.emailjs.com/
2. Создайте Email Service
3. Создайте Email Template
4. Установите пакет:
   ```bash
   npm install @emailjs/browser
   ```

5. В `src/pages/Contact.jsx` замените `handleSubmit`:
   ```javascript
   import emailjs from '@emailjs/browser';
   
   const handleSubmit = (e) => {
     e.preventDefault();
     
     emailjs.send(
       'YOUR_SERVICE_ID',
       'YOUR_TEMPLATE_ID',
       formData,
       'YOUR_PUBLIC_KEY'
     ).then(() => {
       setSubmitStatus('success');
     }).catch(() => {
       setSubmitStatus('error');
     });
   };
   ```

### Вариант 2: Formspree (Бесплатно, 50 отправок/месяц)

1. Зарегистрируйтесь на https://formspree.io/
2. Создайте новую форму
3. Обновите форму в `Contact.jsx`:
   ```javascript
   <form action="https://formspree.io/f/ВАШ_ID" method="POST">
   ```

### Вариант 3: Netlify Forms (если используете Netlify)

1. Добавьте в форму в `Contact.jsx`:
   ```html
   <form name="contact" method="POST" data-netlify="true">
     <input type="hidden" name="form-name" value="contact" />
     {/* остальные поля */}
   </form>
   ```

---

## 🔍 SEO ОПТИМИЗАЦИЯ

### 1. Обновите meta теги в `index.html`:

```html
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  
  <!-- SEO Meta Tags -->
  <title>Ваше Имя - Профессиональный Фотограф | Портфолио</title>
  <meta name="description" content="Профессиональная фотосъемка: портреты, свадьбы, мероприятия. Посмотрите моё портфолио и свяжитесь для заказа." />
  <meta name="keywords" content="фотограф, портретная фотосъемка, свадебный фотограф, ваш город" />
  
  <!-- Open Graph для соцсетей -->
  <meta property="og:title" content="Ваше Имя - Профессиональный Фотограф" />
  <meta property="og:description" content="Профессиональная фотосъемка любой сложности" />
  <meta property="og:image" content="ССЫЛКА_НА_ПРЕВЬЮ_ИЗОБРАЖЕНИЕ" />
  <meta property="og:url" content="https://ваш-сайт.com" />
</head>
```

### 2. Добавьте Google Analytics (опционально):

```html
<!-- В конце <head> -->
<script async src="https://www.googletagmanager.com/gtag/js?id=GA_MEASUREMENT_ID"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());
  gtag('config', 'GA_MEASUREMENT_ID');
</script>
```

---

## 🎨 КАСТОМНЫЙ ДОМЕН

### Netlify:
1. Site settings → Domain management
2. Add custom domain
3. Обновите DNS записи у регистратора

### Vercel:
1. Project settings → Domains
2. Add domain
3. Настройте DNS

---

## ✅ ПРОВЕРКА ПОСЛЕ ДЕПЛОЯ

- [ ] Сайт открывается
- [ ] Все страницы работают
- [ ] Фотографии загружаются
- [ ] Анимации плавные
- [ ] Форма отправляется
- [ ] Работает на мобильных
- [ ] Ссылки на соцсети работают

---

## 🆘 ЧАСТЫЕ ПРОБЛЕМЫ

**Проблема: Страницы не работают (404 при перезагрузке)**
Решение: Настройте редиректы на `index.html` (инструкция выше)

**Проблема: Фото не загружаются**
Решение: Проверьте ссылки, убедитесь что они публичные

**Проблема: Медленная загрузка**
Решение: Оптимизируйте изображения (до 500KB каждое)

---

**Удачи с запуском! 🚀**
